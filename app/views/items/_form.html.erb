<%= form_for [user, item] do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, rows: 8, class: 'form-control', placeholder: "Enter item note" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %> 
